/**
 * Java enum transform
 * @author 余聪
 */
import { parseEnumCode } from './parse-enum-code'

export function javaEnumTransform(code: string) {
  const list = parseEnumCode(code)

  list.concat()
}
